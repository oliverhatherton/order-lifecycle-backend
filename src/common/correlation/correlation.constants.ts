/** HTTP header and message header carrying the correlation id across hops. */
export const CORRELATION_ID_HEADER = 'x-correlation-id';

/** CLS key under which the correlation id is stored for the current context. */
export const CORRELATION_CLS_KEY = 'correlationId';
